{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "startdev": "npm run db:generate && node server.js",
    "build": "prisma generate",
    "postinstall": "prisma generate",
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "npm run db:generate && nodemon server.js",
    "client": "npm run dev --prefix ../frontend",
    "dev": "npm run server",   "db:migrate": "npx prisma migrate dev",
    "db:generate": "npx prisma generate",
    "db:studio": "npx prisma studio",
    "db:seed": "node prisma/seed.js",
    "db:seed-dashboard": "node prisma/seed-dashboard-data.mjs",
    "db:seed-models": "node prisma/seed-models.mjs",
    "db:test": "node prisma/test-db.mjs",
    "create-admin": "node prisma/create-admin.js"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "concurrently": "^9.2.1",
    "prisma": "^7.8.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@prisma/adapter-mariadb": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mariadb": "^3.5.3",
    "multer": "^2.2.0",
    "mysql2": "^3.22.5",
    "nodemon": "^3.1.14"
  }
}
